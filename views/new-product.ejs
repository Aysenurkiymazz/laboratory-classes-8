<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= headTitle %></title>
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body>
    <main>
      <%- include('partials/navigation', {
        menuLinks: menuLinks,
        activeLinkPath: activeLinkPath,
        cartCount: cartCount
      }); %>

      <h1 class="heading">Newest Product</h1>

      <% if (newestProduct?.name) { %>
        <div class="card">
          <img
            class="card__image"
            src="https://source.unsplash.com/random/400x200?product=<%= newestProduct.name %>"
            alt="<%= newestProduct.name %>"
          />
          <h3 class="card__title"><%= newestProduct.name %></h3>
          <p class="card__description"><%= newestProduct.description %></p>
          <p class="card__price">$<%= newestProduct.price.toFixed(2) %></p>
        </div>
      <% } else { %>
        <p>No new products available.</p>
      <% } %>
    </main>
  </body>
</html>
